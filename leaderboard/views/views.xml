<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="leaderboard.list">
      <field name="name">leaderboard list</field>
      <field name="model">leaderboard.leaderboard</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="leaderboard.action_window">
      <field name="name">leaderboard window</field>
      <field name="res_model">leaderboard.leaderboard</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="leaderboard.action_server">
      <field name="name">leaderboard server</field>
      <field name="model_id" ref="model_leaderboard_leaderboard"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <menuitem name="leaderboard" id="leaderboard.menu_root"
              web_icon="src/user/leaderboard/static/description/icon.png"/>

    <!-- menu categories -->
    <menuitem name="Menu 1" id="leaderboard.menu_1" parent="leaderboard.menu_root"/>
    <menuitem name="Menu 2" id="leaderboard.menu_2" parent="leaderboard.menu_root"/>

    <!-- actions -->
    <!--
    <menuitem name="List" id="leaderboard.menu_1_list" parent="leaderboard.menu_1"
              action="leaderboard.action_window"/>
    <menuitem name="Server to list" id="leaderboard" parent="leaderboard.menu_2"
              action="leaderboard.action_server"/>
    -->
  </data>
</odoo>