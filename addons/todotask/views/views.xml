<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="todotask.list">
      <field name="name">todotask list</field>
      <field name="model">todotask.todotask</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="todotask.action_window">
      <field name="name">todotask window</field>
      <field name="res_model">todotask.todotask</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="todotask.action_server">
      <field name="name">todotask server</field>
      <field name="model_id" ref="model_todotask_todotask"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="todotask" id="todotask.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="todotask.menu_1" parent="todotask.menu_root"/>
    <menuitem name="Menu 2" id="todotask.menu_2" parent="todotask.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="todotask.menu_1_list" parent="todotask.menu_1"
              action="todotask.action_window"/>
    <menuitem name="Server to list" id="todotask" parent="todotask.menu_2"
              action="todotask.action_server"/>
    -->
  </data>
</odoo>